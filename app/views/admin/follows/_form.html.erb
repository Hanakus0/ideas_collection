<%= form_with(model: admin_follow) do |form| %>
  <% if admin_follow.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_follow.errors.count, "error") %> prohibited this admin_follow from being saved:</h2>

      <ul>
        <% admin_follow.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
