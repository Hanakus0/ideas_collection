<%= form_with(model: admin_post_genre) do |form| %>
  <% if admin_post_genre.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_post_genre.errors.count, "error") %> prohibited this admin_post_genre from being saved:</h2>

      <ul>
        <% admin_post_genre.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
