<%= link_to t('profile.unfollowing'), users_follow_path(user.id), class: "flex items-center bg-blue-600 hover:bg-blue-700 text-gray-100 px-4 py-2 rounded text-sm space-x-2 transition duration-100", data: { turbo_method: :delete }, id: "unfollow-btn-#{user.id}" %>
