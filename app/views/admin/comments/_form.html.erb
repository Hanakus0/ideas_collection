<%= form_with(model: admin_comment) do |form| %>
  <% if admin_comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_comment.errors.count, "error") %> prohibited this admin_comment from being saved:</h2>

      <ul>
        <% admin_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :post_id, style: "display: block" %>
    <%= form.text_field :post_id %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
