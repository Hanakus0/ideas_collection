<% if user_signed_in? %>
  <% content_for :title, "#{t('title.app')}" %>
  <%= render 'after_home' %>
<% else %> 
  <% content_for :title, "#{t('title.app')} | home" %>
  <%= render 'before_home' %>
<% end %> 
