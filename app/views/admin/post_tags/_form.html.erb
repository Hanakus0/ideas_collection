<%= form_with(model: admin_post_tag) do |form| %>
  <% if admin_post_tag.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_post_tag.errors.count, "error") %> prohibited this admin_post_tag from being saved:</h2>

      <ul>
        <% admin_post_tag.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :post_id, style: "display: block" %>
    <%= form.text_field :post_id %>
  </div>

  <div>
    <%= form.label :tag_id, style: "display: block" %>
    <%= form.text_field :tag_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
